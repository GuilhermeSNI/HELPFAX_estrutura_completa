HELPFAX pronto para GitHub Pages + backend Node.js + PostgreSQL.

O frontend usa `api-config.js` para apontar para a API.
O backend está em `server.js`.
Não coloque `DATABASE_URL` ou `JWT_SECRET` no GitHub.
Consulte `DEPLOY.md`.
